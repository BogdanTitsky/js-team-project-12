// const key = 'key';
// btnEl.addEventListener('click', e => {
//   e.preventDefault();
//   localStorage.setItem(key, //сюда вторым аргументом объект фильма// );
//   const storage = localStorage.getItem(key);
//   console.log(storage);

// });
